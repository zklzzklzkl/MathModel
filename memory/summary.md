# Experience Memory Summary

This file stores distilled, commit-safe lessons from completed math-modeling projects.

- Raw event logs live in `memory/events.jsonl` and are intentionally ignored.
- Add only reviewed summaries here: reusable lessons, not private project traces.
- Use `python scripts/memory_brief.py --query "<topic>"` before a phase to retrieve a small briefing.
