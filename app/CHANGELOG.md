# Changelog

## 0.2.0

- Added beginner-oriented Control Center documentation.
- Added workspace creation, source upload, richer artifact preview, phase readiness checks, revision task generation, run history, and harness prepare flow.
- Added safe copied runs for Manual/Codex/Claude Code/OpenCode prompt workflows.
- Updated `start.bat` to skip dependency installation when dependencies are already present and retry pip installation with a mirror if PyPI times out.
- Added ignore rules for local workspaces, uploaded source files, run histories, environments, dependencies, and build outputs.

## 0.1.0

- Initial FastAPI + Vue local Control Center.
