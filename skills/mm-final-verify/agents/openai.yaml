interface:
  display_name: "Math Modeling Final Verify"
  short_description: "Verify that a contest paper is traceable, visual, reproducible, and submission-ready."
  default_prompt: "Use $mm-final-verify to perform final acceptance checks for a math modeling contest deliverable."
policy:
  allow_implicit_invocation: true
