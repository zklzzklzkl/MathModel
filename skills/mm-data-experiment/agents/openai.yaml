interface:
  display_name: "Math Modeling Experiment"
  short_description: "Run reproducible experiments and generate figures for contest papers."
  default_prompt: "Use $mm-data-experiment to implement the approved model, run experiments, and generate traceable figures."
policy:
  allow_implicit_invocation: true
